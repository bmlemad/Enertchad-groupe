{
  "name": "enertchad-dashboard-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build pipeline for dashboard-executif.jsx (JSX → minified JS bundle). No React/Recharts bundled — they are loaded as self-hosted UMD scripts in the HTML.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.24.0",
    "tailwindcss": "3.4.14",
    "cssnano": "7.0.6",
    "postcss": "8.4.47"
  }
}
